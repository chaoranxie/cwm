{
  "rules": {
    "routes": {
      ".read": true,
      ".write": "auth !== null"
    },
    "routeCompletions": {
      ".read": false,
      ".write": false,
      "$uid": {
        ".write": "$uid === auth.uid",
        ".read": "$uid === auth.uid"
      }
    }
  }
}
